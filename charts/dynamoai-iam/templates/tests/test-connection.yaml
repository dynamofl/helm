apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dynamoai.fullname" . }}-test-connection"
  labels:
    {{- include "dynamoai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dynamoai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
